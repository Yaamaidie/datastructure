package exercise.ch4;

public class Ex4_14 {
	/*
	 * a.构造一个boolean数组B，长度为M，如果i在树里，B[i]为true，否则为false。
	 * 重复生成（1，M）间的随机数，直到找到树中未包含的数（条件为B[j]=false）。
	 * 如果已经有N个数在树中，则M-N个没在树中，则找到树上存在的概率为(M-N)/M，
	 * 运行时间取倒数为M/(M-N) = a/(a-1)
	 * b.概率为：N/M --> 运行时间为：M/N = a
	 * c.
	 */
}
