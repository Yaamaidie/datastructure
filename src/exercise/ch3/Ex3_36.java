package exercise.ch3;
/**
 * 假设我们有到单链表某个节点的一个引用，而且保证他不是最后一个节点。我们没有到任何其他节点的引用(除非是通过后面的一些next链)，描述一个O(1)的算法，该算法从
 * 该链表删除存储在这样一个节点的值，同事保持链表的完整性。(提示：涉及下一个节点)。
 * @author lee
 *
 */
public class Ex3_36 {
	//Node<T> reference;
	//拷贝next的值到reference：reference.data = reference.next.data;
	//删除next节点，并将reference指向next.next节点
}
